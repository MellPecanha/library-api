
## 📚 Library Manager API

Library Manager is a simple API built with **Node.js**, **TypeScript**, **Prisma**, and **esbuild** to manage a library’s book collection.

### Features

- **Create books** with the following fields:
  - `title`
  - `author`
  - `ISBN`
  - `publisherYear`
  - `publisher`

- **Update book information**

- **List all books**

- **List books by publisher**

---

This project focuses on clean structure, type safety with TypeScript, and efficient builds using esbuild.
